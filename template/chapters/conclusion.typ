#import "@preview/bookly:5.1.1": *

= 结语

本样板展示了 Typst 与 Bookly 组合用于中文书籍排版的完整效果。接下来只需三步即可把内容搬进正式项目：替换 `main.typ` 顶部的书名与作者；把各章内容写入 `chapters/` 目录；把参考文献条目录入 `bibliography/sample.bib`。

#info-box[把 `main.typ` 中的 `lang` 改为其他语言时，Bookly 会自动切换界面标签。目前内置中文、英语、法语、德语、西班牙语、意大利语与葡萄牙语七种语言。]

